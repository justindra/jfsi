# Just Fucking Ship It - React!

This uses tailwind so update the `tailwind.config.js` with the following:

```ts
module.exports = {
  content: [
    ...,
    './node_modules/@jfsi/react/**/*.js',
  ],
    ...
};

```
